# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# native (regenerated by `expo prebuild` / CNG)
/ios
/android

# secrets & env — NEVER commit these
.env
.env.local
.env*.local
.env.seed

# macOS
.DS_Store

# misc
*.log
